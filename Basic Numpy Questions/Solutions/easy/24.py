# 24
# یک ماتریس شش در شش بسازید و سپس تمام اعداد حاشیه ای را یعنی سطر و ستون اول و آخر به صورت یک بردار بازگردانید عناصر گوشه ای نباید تکرار شود اما ترتیب اهمیتی ندارد

import numpy as np
y = np.arange(0,36).reshape(6,6)
print(y)
print("---------------")
y1 = y[0:6:5, 0:]
print(y1)
print("---------------")
y2 = y[:, 0:6:5].T
print(y2)
print("---------------")
result = np.hstack((y1, y2[:,1:5]))    
print(result.reshape(1,20))
