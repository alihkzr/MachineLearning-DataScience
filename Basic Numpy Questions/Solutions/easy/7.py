# 7
# یک ماتریس سه در چهار که با اعداد تصادفی بین منفی صد و صد پر شده است را بسازید و سپس انحراف معیار هر ستون را محاسبه کرده و هر ستون را محاسبه کرده و هر ستون را بر این مقدار تقسیم کند پس از انجام این کار باید انحراف معیار هر ستون یک شود

import numpy as np
g = np.random.randint(-100,100,(3,4))
print(g)
print(g.std(axis=0).round())
g = g/g.std(axis=0).round()
print(g)
print(g.std(axis=0).round())
